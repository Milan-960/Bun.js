{
  "name": "bun-first",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "build": "bun build ./index.ts --outdir ./dist --watch",
    "test": "bun test index.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.1.14",
    "@types/figlet": "^1.7.0"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "figlet": "^1.8.0"
  }
}
